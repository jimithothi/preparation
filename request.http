### Register User
POST http://localhost:3000/api/auth/register
Content-Type: application/json

{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "Password123"
}

### Login User
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "email": "john@example.com",
  "password": "Password123"
}


### GET Users
GET http://localhost:3000/api/users
Content-Type: application/application/json



### GET Questions
GET http://localhost:3000/api/questions?tags=javascript,api
Content-Type: application/application/json


### CREATE Question
POST http://localhost:3000/api/question
Content-Type: application/application/json

{
  "question": "What is the difference between SQL and NoSQL databases?",
  "answer": "<p>SQL and NoSQL databases differ in their data structure, scalability, and use cases.</p><h3>SQL (MySQL, PostgreSQL)</h3><ul><li>Uses tables with rows and columns</li><li>Requires predefined schema</li><li>Better for complex queries and relationships</li><li>ACID compliant</li></ul><h3>NoSQL (MongoDB)</h3><ul><li>Uses collections of documents</li><li>Schema-less and flexible</li><li>Better for scalability and big data</li><li>Eventually consistent</li></ul><h3>Example: Storing User Data</h3><pre><code>// SQL (MySQL/PostgreSQL)\nCREATE TABLE users (\n  id INT PRIMARY KEY,\n  name VARCHAR(100),\n  email VARCHAR(100),\n  created_at TIMESTAMP\n);\n\nINSERT INTO users (id, name, email) VALUES (1, 'John', 'john@example.com');\n\n// NoSQL (MongoDB)\ndb.users.insertOne({\n  name: 'John',\n  email: 'john@example.com',\n  created_at: new Date()\n});</code></pre>",
  "category": "Technical",
  "tags": ["database"]
}

### PUT Question
PUT http://localhost:3000/api/question/68e2c713333cc9b553ed8104
Content-Type: application/application/json

{
  "question": "What is the difference between let, const, and var in JavaScript?",
  "answer": "var is function-scoped and can be redeclared. let is block-scoped and cannot be redeclared. const is block-scoped, cannot be redeclared, and cannot be reassigned (though objects/arrays it points to can be mutated).",
  "category": "Technical",
  "tags": ["javascript", "es6", "variables"]
}


### BULK CREATE Question
POST http://localhost:3000/api/question/bulk
Content-Type: application/application/json

{
  "questions": [
    {
      "question": "What is the difference between SQL and NoSQL databases?",
      "answer": "SQL databases are relational with structured schemas and use SQL for queries. NoSQL databases are non-relational, schema-flexible, and optimized for specific data models like documents, key-value, or graphs.",
      "category": "Technical",
      "tags": ["databases", "sql", "nosql"]
    },
    {
      "question": "Explain the concept of RESTful API design",
      "answer": "RESTful APIs follow REST principles using HTTP methods (GET, POST, PUT, DELETE), stateless communication, and resource-based URLs. They use standard status codes and typically return JSON data.",
      "category": "Technical",
      "tags": ["api", "rest", "backend"]
    },
    {
      "question": "What is the purpose of Git branching?",
      "answer": "Git branching allows developers to create isolated workspaces for features or fixes without affecting the main codebase. Branches can be merged back once work is complete, enabling parallel development and safer experimentation.",
      "category": "Technical",
      "tags": ["git", "version-control", "collaboration"]
    }
  ]
}